import 'package:flutter/material.dart';

class AlphaBoundStatsSheet extends StatelessWidget {
  const AlphaBoundStatsSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(),
    );
  }
}
